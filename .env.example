# Jira credentials for scripts in this repo (e.g. update_user_stories.py)
# Copy this file to `.env` and fill in real values.
#
# NOTE: Do not commit `.env` (it should contain secrets).

# Base URL of your Jira site (no trailing slash)
JIRA_BASE_URL="https://gnapartners.atlassian.net"

# Jira user email (used for Basic auth)
JIRA_EMAIL="you@gnapartners.com"

# Jira API token generated from Atlassian account settings
JIRA_API_TOKEN="replace-with-your-api-token"
